{
  "name": "aci-motors-edge",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "basic-ftp": "^6.0.1",
    "hono": "^4.0.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
